[Unit]
Description=CoreDNS for the lab.
After=network.target

[Service]
PrivateTmp=true
Type=simple
ExecStart=/usr/local/bin/coredns \
    -conf /etc/coredns/Corefile

[Install]
WantedBy=multi-user.target
